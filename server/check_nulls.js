const { createClient } = require('@supabase/supabase-js'); require('dotenv').config(); const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); supabase.from('AuditLogs').select('*').is('Type', null).order('Timestamp', { ascending: false }).limit(5).then(res => console.log(res.data)).catch(console.error);
